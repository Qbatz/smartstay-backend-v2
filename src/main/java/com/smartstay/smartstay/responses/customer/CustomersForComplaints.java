package com.smartstay.smartstay.responses.customer;

public record CustomersForComplaints(String customerId,
                                     String fullName,
                                     String firstName,
                                     String lastName,
                                     String profilePic,
                                     String initials) {
}
